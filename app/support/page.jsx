'use client';
import ComingSoon from '@/components/ComingSoon';

export default function SupportPage() {
  return (
    <ComingSoon
      title="Support."
      current="support"
      body="Ticket form + FAQ. Submission writes to Supabase + emails joan@xovnd.com via Resend. Phase 2B.7."
    />
  );
}
